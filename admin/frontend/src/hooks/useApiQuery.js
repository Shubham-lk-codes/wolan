import { useCallback, useEffect, useState } from 'react'; import { getApiError } from '../services/api';
export function useApiQuery(loader,initialValue){const[data,setData]=useState(initialValue),[loading,setLoading]=useState(true),[error,setError]=useState(''),[version,setVersion]=useState(0);const refresh=useCallback(()=>setVersion(v=>v+1),[]);useEffect(()=>{const controller=new AbortController();setLoading(true);loader(controller.signal).then(setData).catch(error=>{if(error.code!=='ERR_CANCELED')setError(getApiError(error));}).finally(()=>setLoading(false));return()=>controller.abort();},[loader,version]);return{data,setData,loading,error,refresh};}
